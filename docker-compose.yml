
services:
  backend:
    build: .
    container_name: gym_backend_container
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/code/backend
    env_file:
      - ./backend/.env
    command: uvicorn backend.app.main:app --host 0.0.0.0 --port 8000 --reload